server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  profiles:
    active: native #native表示配置文件在本地
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared